#include "main.h"

/**
 * mul - multiplies two ints.
 * @a: first integer
 * @b: second integer
 * Return: pro (product of a & b)
 */

int mul(int a, int b)
{
	int pro;

	pro = a * b;
	return (pro);
}
