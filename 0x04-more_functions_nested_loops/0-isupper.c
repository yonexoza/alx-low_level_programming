#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: input integral value to check.
 * Return: 1 (c is uppercased) or 0 otherwise.
 */

int _isupper(int c)
{
	return (c >= 65 && c < 91);
}
